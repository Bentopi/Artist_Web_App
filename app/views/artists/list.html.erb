<div class="container">
  <h1>Search Results for: <%= params[:artist_name] %></h1>
  <hr>
   <% @artists.each do |artist| %>
     <%= link_to artist.name, artist_path(artist.id), class: "album-list" %> <br>
   <% end %>
</div>
